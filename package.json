{
  "name": "upload-project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "install-all": "cd upload-mobile && yarn && cd ../upload-server && yarn && cd ../upload-web && yarn"
  }
}
